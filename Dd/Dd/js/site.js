document.getElementById('folderInput').addEventListener('change', function(e) {
    var selectedFolder = e.target.files[0].webkitRelativePath;
    // Assign the selected folder path to a hidden input field or display it to the user
});
